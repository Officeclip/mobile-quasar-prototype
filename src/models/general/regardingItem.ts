export interface regarding{
  type:regardingType,
  value: regardingItem,
}
export interface regardingType {
  id: string;
  name: string;
}

export interface regardingItem {
  id: string;
  name: string;
}
