{
  "extends": "./.quasar/tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "src/*": ["src/*"], // This is the key line
      "app/*": ["./*"] // Quasar might have this too
    }
  },
  "allowJs": true,
  "_comment": "FIXME: need to remove allowJs"
}
